# Rediseño visual v2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar el rediseño visual aprobado del portfolio: identidad terminal-native, hero con fondo glow↔ola-ASCII, skills radar, bento de proyectos con preview web, HTB/GitHub re-estilizados, social fastfetch con avatares ASCII y paleta ⌘K con arte ASCII de `/public/art`.

**Architecture:** Componentes client de Next.js 14. Un pipeline `canvas→ASCII` reutilizable (`app/lib/ascii.ts`) alimenta social y ⌘K. El hero usa su propio canvas de flow-field. Sin librerías nuevas. Datos reales de `/api/htb` y `/api/github` (existentes).

**Tech Stack:** Next.js 14.1, TypeScript, Tailwind 3.3, React 18, canvas/SVG nativos.

**Fuente de verdad visual:** los mockups aprobados están en el repo:
- `docs/superpowers/mockups/final.html` — página completa (hero, skills radar, projects bento, HTB, GitHub, social, ⌘K). **CSS y JS de cada sección a portar desde aquí.**
- `docs/superpowers/mockups/hero-wave.html` — hero con la ola de caracteres + crossfade.
- `docs/superpowers/mockups/skills-radar.html` — radar (SVG estático).
- `docs/superpowers/mockups/ascii-social-ctrlk.html` — social + ⌘K con canvas→ASCII.

Spec: `docs/superpowers/specs/2026-06-19-portfolio-redesign-v2-design.md`.

## Global Constraints

- Sin dependencias nuevas ni framework de tests.
- No tocar `/admin`, auth, analytics (fases 2-3).
- Paleta: fondo `#08080b`, morado `#8b5cf6`, azul `#3b82f6`, verde `#22c55e` SOLO para prompts, color por lenguaje en projects/skills.
- Tipografía: Sora (display) + JetBrains Mono (mono). Ya cargadas en `layout.tsx`.
- Eyebrows de comando (`$ ...`) en cada sección.
- `prefers-reduced-motion`: pausar/anular ola del hero, marquees y reveals.
- Mantener el atajo backtick (`` ` ``) de la terminal y el ⌘K existente.
- Imágenes para ASCII deben ser **mismo-origen** (canvas no se "tainta"): avatares en `public/avatars/`, arte en `public/art/`, screenshots en `public/projects/`.
- Gate por tarea (sin test runner): `npm run build` sin errores + verificación manual en `npm run dev`. Un commit por tarea.

---

## File Structure

**Nuevos**
- `app/lib/ascii.ts` — pipeline canvas→ASCII reutilizable.
- `app/components/HeroBackground.tsx` — canvas flow-field + crossfade.
- `app/components/sections/Social.tsx` — fastfetch social.
- `public/avatars/.gitkeep`, `public/art/.gitkeep`, `public/projects/.gitkeep` (placeholders).

**Reescritos**
- `app/components/sections/Hero.tsx`, `Skills.tsx`, `Projects.tsx`, `HTB.tsx`, `GitHub.tsx`
- `app/components/CommandPalette.tsx`
- `app/globals.css` (tokens/utilidades del nuevo lenguaje)
- `app/page.tsx` (montaje de Social en vez de Contact; resto igual)
- `app/components/Navbar.tsx` (enlace a #github/#contact, scroll-spy ya existe)

**Posible retiro:** `app/components/sections/Contact.tsx` (sustituido por `Social.tsx`).

---

## Task 1: Tokens visuales + pipeline ASCII

**Files:** Modify `app/globals.css`; Create `app/lib/ascii.ts`.

**Interfaces:**
- Produces (`ascii.ts`):
  ```ts
  export function toAscii(src: HTMLCanvasElement, cols: number, rows: number): string;
  export function loadImageToCanvas(url: string, w?: number, h?: number): Promise<HTMLCanvasElement>; // same-origin
  ```
- Produces (CSS): utilidades nuevas reutilizadas por las demás tareas: `.eyebrow`, `.seclabel`, `.tagk`, `.card-glass`/`.card-hover` (ya existen de Fase 1; verificar), tokens de color por lenguaje vía variable `--ac`.

- [ ] **Step 1: Crear `app/lib/ascii.ts`**

```ts
'use client';

/** Downsamplea un canvas a una rejilla cols×rows y mapea luminancia a una rampa ASCII. */
export function toAscii(src: HTMLCanvasElement, cols: number, rows: number): string {
  const ctx = src.getContext('2d');
  if (!ctx) return '';
  const d = ctx.getImageData(0, 0, src.width, src.height).data;
  const ramp = ' .:-=+*#%@';
  const sx = src.width / cols, sy = src.height / rows;
  let out = '';
  for (let j = 0; j < rows; j++) {
    for (let i = 0; i < cols; i++) {
      const x0 = (i * sx) | 0, x1 = ((i + 1) * sx) | 0;
      const y0 = (j * sy) | 0, y1 = ((j + 1) * sy) | 0;
      let s = 0, n = 0;
      for (let y = y0; y < y1; y += 2)
        for (let x = x0; x < x1; x += 2) {
          const o = (y * src.width + x) * 4;
          s += 0.299 * d[o] + 0.587 * d[o + 1] + 0.114 * d[o + 2];
          n++;
        }
      const l = n ? s / n : 0;
      out += ramp[Math.min(ramp.length - 1, ((l / 256) * ramp.length) | 0)];
    }
    out += '\n';
  }
  return out;
}

/** Carga una imagen mismo-origen en un canvas (para luego pasarla a toAscii). */
export function loadImageToCanvas(url: string, w = 160, h = 160): Promise<HTMLCanvasElement> {
  return new Promise((resolve, reject) => {
    const img = new Image();
    img.onload = () => {
      const c = document.createElement('canvas');
      c.width = w; c.height = h;
      const x = c.getContext('2d');
      if (!x) return reject(new Error('no ctx'));
      x.fillStyle = '#0a0a12'; x.fillRect(0, 0, w, h);
      x.drawImage(img, 0, 0, w, h);
      resolve(c);
    };
    img.onerror = reject;
    img.src = url;
  });
}
```

- [ ] **Step 2: Añadir utilidades a `app/globals.css`**

Portar del `<style>` de `docs/superpowers/mockups/final.html` las clases del nuevo lenguaje que aún no existan: `.eyebrow` (con `::before { content:"$ " }`), `.seclabel`, `.tagk`. Mantener intactas `.card-glass`/`.card-accent`/`.text-gradient`/`.reveal*` de Fase 1. No duplicar.

- [ ] **Step 3: Crear placeholders de assets**

```bash
mkdir -p public/avatars public/art public/projects
printf '' > public/avatars/.gitkeep
printf '' > public/art/.gitkeep
printf '' > public/projects/.gitkeep
```

- [ ] **Step 4: Build + Commit**

Run: `npm run build` → OK.
```bash
git add app/lib/ascii.ts app/globals.css public/avatars/.gitkeep public/art/.gitkeep public/projects/.gitkeep
git commit -m "feat(redesign): add canvas->ASCII pipeline and design tokens"
```

---

## Task 2: Hero + fondo ola de caracteres

**Files:** Create `app/components/HeroBackground.tsx`; Rewrite `app/components/sections/Hero.tsx`.

**Interfaces:**
- Consumes: nada (canvas propio).
- Produces: `Hero` con layout a la izquierda, eyebrow `$ whoami`, título gradiente, botones (uno abre terminal via prop `onOpenTerminal`), chips. Fondo con crossfade glow↔ola.

- [ ] **Step 1: Crear `app/components/HeroBackground.tsx`**

Portar el canvas flow-field y el crossfade de `docs/superpowers/mockups/hero-wave.html` (función `field`, loop `draw`, `resize`, alternancia de `opacity` cada 8s). Adaptaciones obligatorias:
- Componente client con `useRef` al `<canvas>` y al contenedor; `useEffect` que arranca el `requestAnimationFrame` y el `setInterval` de 8s, y los limpia al desmontar.
- Respetar `prefers-reduced-motion`: si activo, no animar la ola (mostrar solo glow, o capa estática) y no crear el interval.
- Pausar el `requestAnimationFrame` cuando el hero no está en viewport (IntersectionObserver) para ahorrar CPU.
- Renderiza: `<div className="bgGlow"/>`, `<canvas/>`, `<div className="veil"/>` con la misma CSS del mockup (portar las clases `.bg`, `.bgGlow`, `#wave`, `.veil` a `globals.css` o a estilos locales).

- [ ] **Step 2: Reescribir `app/components/sections/Hero.tsx`**

Portar el markup del hero de `final.html` (`#about`): eyebrow `whoami` + cursor, `<h1>` con `s7lver` en `.text-gradient`, botones `View Projects →` (scroll a #projects) y `$ start hacking` (llama `onOpenTerminal`), chips. Montar `<HeroBackground/>` como fondo absoluto y el contenido en `z-index:2`. Mantener la prop `onOpenTerminal` que ya recibe desde `page.tsx`.

- [ ] **Step 3: Build + manual**

Run: `npm run build` → OK.
En dev: hero a la izquierda; el fondo alterna glow↔ola cada ~8s; el texto se lee bien; con reduced-motion la ola no anima.

- [ ] **Step 4: Commit**

```bash
git add app/components/HeroBackground.tsx app/components/sections/Hero.tsx app/globals.css
git commit -m "feat(redesign): hero with crossfading glow/ASCII-wave background"
```

---

## Task 3: Skills — radar

**Files:** Rewrite `app/components/sections/Skills.tsx`.

**Interfaces:**
- Produces: sección `#skills` con el radar SVG + leyenda.

- [ ] **Step 1: Reescribir `Skills.tsx`**

Portar de `docs/superpowers/mockups/skills-radar.html` la **Opción 5** (radar): el `<svg>` con anillos, ejes, polígono de datos, puntos y etiquetas, y la leyenda lateral (`.rl`). Definir los ejes/valores/herramientas como un array tipado en el componente:
```ts
const AXES = [
  { name: 'Web Exploit', value: 0.92, color: '#f87171', tools: 'Burp · SQLMap · Wfuzz' },
  { name: 'Network',     value: 0.85, color: '#22d3ee', tools: 'Nmap · Wireshark · tcpdump' },
  { name: 'Recon',       value: 0.80, color: '#a3e635', tools: 'Amass · Subfinder · OSINT' },
  { name: 'Active Dir',  value: 0.75, color: '#8b5cf6', tools: 'BloodHound · Impacket' },
  { name: 'Reversing',   value: 0.68, color: '#f472b6', tools: 'Ghidra · gdb · radare2' },
  { name: 'Crypto',      value: 0.60, color: '#fde047', tools: 'hashcat · John' },
];
```
Calcular los puntos del polígono con un helper (centro 150,150, R=110, ángulo `-90° + i*60°`), en vez de hardcodear coordenadas. Eyebrow `$ cat skills.md`. Aplicar `useReveal` al bloque.

- [ ] **Step 2: Build + manual + Commit**

Run: `npm run build` → OK. En dev: radar de 6 ejes + leyenda con % y herramientas.
```bash
git add app/components/sections/Skills.tsx
git commit -m "feat(redesign): skills radar chart"
```

---

## Task 4: Projects — bento irregular + preview web

**Files:** Rewrite `app/components/sections/Projects.tsx`; add screenshots to `public/projects/`.

**Interfaces:**
- Produces: sección `#projects` con el bento animado; proyectos con `web` muestran screenshot de fondo al hover.

- [ ] **Step 1: Definir datos de proyectos**

En `Projects.tsx`, array tipado:
```ts
type Project = { slug: string; name: string; desc: string; status: 'done'|'beta'|'dev'; ac: string; tags: string[]; web?: string; shot?: string; span: 'p1'|'p2'|'p3'|'p4' };
```
Rellenar con file-meet, ZephyrOS, tsuki, CodeDotJS (datos reales del repo actual `Projects.tsx`), marcando `web`/`shot` los que tengan página. Quinta celda = commits + sparkline (`p5`).

- [ ] **Step 2: Portar el bento**

Portar de `docs/superpowers/mockups/final.html` (sección Projects) el CSS `.bento`, `.tile`, `.p1..p5`, las reglas `:has(...)` de redistribución de columnas, `.shot`/`.shade` (con `.tile>*:not(.shot):not(.shade){position:relative;z-index:2}`), `.de` reveal, `.live`, `.path`, `.tw/.tg`, `.spark`. Renderizar las tiles desde el array. Para `web`, poner `<div className="shot">` con `style={{ backgroundImage: shot ? \`url(${shot})\` : undefined }}` + `<div className="shade"/>` + badge `↗ live`.
- Screenshots reales: guardar `public/projects/<slug>.png` y referenciarlas en `shot`. Si no hay screenshot aún, usar un placeholder gris en `public/projects/placeholder.png` y documentar que se sustituyan.

- [ ] **Step 3: Crear placeholder de screenshot**

Añadir `public/projects/placeholder.png` (una imagen oscura simple). Si no se puede generar binario, dejar `shot` undefined y que el hover solo expanda (documentar en el commit).

- [ ] **Step 4: Build + manual + Commit**

Run: `npm run build` → OK. En dev: bento irregular; al hover se redistribuyen columnas, se atenúan los demás, se revela descripción; los `↗ live` muestran su screenshot de fondo.
```bash
git add app/components/sections/Projects.tsx public/projects/
git commit -m "feat(redesign): animated irregular projects bento with web preview"
```

---

## Task 5: HTB — KPIs + barras

**Files:** Rewrite `app/components/sections/HTB.tsx`.

**Interfaces:**
- Consumes: `GET /api/htb` (existente; conservar el fetch y el parseo actuales).
- Produces: sección `#htb` re-estilizada.

- [ ] **Step 1: Reescribir `HTB.tsx`**

Mantener la lógica de fetch/estado actual de `HTB.tsx`. Reemplazar el render por el de `final.html` (sección HTB): `.row4` de KPIs (Rank gradiente, User/System owns, Points) + `.row2` con dos `.card` de barras (`.bar/.track/.fill` con animación de ancho) para dificultad y SO. Mapear los datos reales de la API a estas barras. Eyebrow `$ htb --stats`. `useReveal` al bloque.

- [ ] **Step 2: Build + manual + Commit**

Run: `npm run build` → OK. En dev (con env HTB): KPIs y barras con datos reales; estados loading/error conservados.
```bash
git add app/components/sections/HTB.tsx
git commit -m "feat(redesign): restyle HTB stats (KPIs + bars)"
```

---

## Task 6: GitHub — bento + heatmap

**Files:** Rewrite `app/components/sections/GitHub.tsx`.

**Interfaces:**
- Consumes: `GET /api/github` (existente).
- Produces: sección `#github` re-estilizada.

- [ ] **Step 1: Reescribir `GitHub.tsx`**

Conservar el fetch/estado actual. Render del `final.html` (sección GitHub): `.ghbento` con `.heatbig` (heatmap + barra de lenguajes) + tiles KPI (repos, stars, followers, commits/yr). El heatmap se genera con el pequeño script del mockup (rejilla de celdas con intensidad) — portarlo a un `useMemo`/render React (array de celdas con su color). Usar `data.languages` reales para la barra; KPIs reales. Eyebrow `$ git log --stat`. Nota: el heatmap es decorativo salvo que haya `GITHUB_TOKEN`+GraphQL (documentado en spec).

- [ ] **Step 2: Build + manual + Commit**

Run: `npm run build` → OK. En dev: bento con heatmap + KPIs + lenguajes reales.
```bash
git add app/components/sections/GitHub.tsx
git commit -m "feat(redesign): restyle GitHub section (bento + heatmap)"
```

---

## Task 7: Social — fastfetch con avatares ASCII

**Files:** Create `app/components/sections/Social.tsx`; Modify `app/page.tsx`; (retirar `Contact.tsx`).

**Interfaces:**
- Consumes: `toAscii`/`loadImageToCanvas` (Task 1); avatares en `public/avatars/<red>.png`.
- Produces: sección `#contact` fastfetch navegable.

- [ ] **Step 1: Crear `Social.tsx`**

Portar de `docs/superpowers/mockups/ascii-social-ctrlk.html` (sección Social) y de `final.html` (#contact): ventana `.ff`, `.art` (ASCII), `.list` de redes `.li` navegable (hover/↑↓/click), `.fb` feedback. Datos:
```ts
const SOCIALS = [
  { k:'github', v:'github.com/s7lver', color:'#6e5494', avatar:'/avatars/github.png', url:'https://github.com/s7lver' },
  { k:'discord', v:'@s7lver', color:'#5865f2', avatar:'/avatars/discord.png', url:'#' },
  // x, tiktok, instagram, htb...
];
```
Generación ASCII: en `useEffect`, para cada red `loadImageToCanvas(avatar)` → `toAscii(canvas, 28, 14)`; cachear en estado. Si falla la carga, ASCII placeholder. El avatar seleccionado se muestra en `.art` coloreado con el color de la red. Teclado: ↑↓ cambia selección (solo cuando el ⌘K no está abierto), ↵/click abre `url`. Mantener email CTA.

- [ ] **Step 2: Montar en `page.tsx`**

Sustituir `import ContactSection` por `import Social from '@/components/sections/Social'` y renderizar `<Social/>` donde iba Contact. Eliminar `Contact.tsx` si queda sin uso.

- [ ] **Step 3: Avatares placeholder**

Añadir avatares mismo-origen en `public/avatars/` (placeholders si no hay reales) y documentar que se sustituyan por los avatares reales de cada cuenta.

- [ ] **Step 4: Build + manual + Commit**

Run: `npm run build` → OK. En dev: lista de redes; al navegar, el ASCII muestra el avatar de cada cuenta; ↵/click abre el enlace.
```bash
git add app/components/sections/Social.tsx app/page.tsx public/avatars/
git rm app/components/sections/Contact.tsx 2>/dev/null || true
git commit -m "feat(redesign): social fastfetch with canvas->ASCII avatars"
```

---

## Task 8: Command Palette ⌘K con arte ASCII

**Files:** Rewrite `app/components/CommandPalette.tsx`; add images to `public/art/`.

**Interfaces:**
- Consumes: `toAscii`/`loadImageToCanvas` (Task 1); imágenes en `public/art/`; lista de secciones.
- Produces: paleta ⌘K con panel ASCII aleatorio + navegación de secciones.

- [ ] **Step 1: Reescribir `CommandPalette.tsx`**

Partir del existente (Fase 1) y portar de `ascii-social-ctrlk.html` (overlay ⌘K): layout `.pal` con `.palart` (ASCII) + `.pallist` (secciones navegables) + `.palfoot`. Secciones: Home/Skills/Projects/HTB/GitHub/Contact → `scrollIntoView`. Al abrir, elegir **una imagen al azar de `/public/art`** → `loadImageToCanvas` → `toAscii(canvas, 30, 15)` → mostrar. Mantener props `{ open, onClose }` y el control por ⌘K en `page.tsx` (ya existe). Lista de imágenes de `/public/art`: definir un manifest `const ART = ['/art/1.png','/art/2.png', ...]` (o leerlas en build).

- [ ] **Step 2: Imágenes placeholder en `public/art/`**

Añadir 2-3 imágenes en `public/art/` (placeholders) y el manifest. Documentar que el usuario añada las suyas.

- [ ] **Step 3: Build + manual + Commit**

Run: `npm run build` → OK. En dev: ⌘K abre la paleta; el ASCII de la izquierda cambia (aleatorio) en cada apertura; navegar con ↑↓/↵/click hace scroll a la sección.
```bash
git add app/components/CommandPalette.tsx public/art/
git commit -m "feat(redesign): command palette with random /public/art ASCII"
```

---

## Task 9: Integración, navbar y verificación final

**Files:** Modify `app/components/Navbar.tsx`, `app/globals.css`; verify `app/page.tsx`.

- [ ] **Step 1: Navbar**

Asegurar enlaces a `#about/#skills/#projects/#htb/#github/#contact`, scroll-spy (ya existe de Fase 1) cubriendo los ids actuales, hint `⌘K`. Ajustar ids si cambiaron (hero ahora `#about`).

- [ ] **Step 2: Repaso de coherencia**

Verificar que todas las secciones usan el contenedor/espaciado del nuevo lenguaje (`.sec`, `.wrap`, eyebrows), sin estilos viejos sueltos. Quitar CSS muerto en `globals.css`.

- [ ] **Step 3: Build + verificación integral**

Run: `npm run build` → OK.
Checklist en `npm run dev` (con `GITHUB_USERNAME` y env de HTB):
- Hero: layout izquierda + crossfade glow/ola; reduced-motion pausa la ola.
- Skills radar; Projects bento + preview web; HTB KPIs/barras; GitHub bento/heatmap con datos reales.
- Social fastfetch muestra avatares ASCII; ↵ abre enlaces.
- ⌘K abre paleta con arte ASCII aleatorio y navega.
- Navbar resalta sección activa; responsive móvil; sin errores de consola.

- [ ] **Step 4: Commit**

```bash
git add app/components/Navbar.tsx app/globals.css
git commit -m "feat(redesign): navbar/ids integration and visual cleanup"
```

---

## Self-Review (cobertura de la spec)

- §2 Hero (ola + crossfade) → Task 2. ✅
- §3 Skills radar → Task 3. ✅
- §4 Projects bento + preview web → Task 4. ✅
- §5 HTB → Task 5. ✅
- §6 GitHub → Task 6. ✅
- §7 Social fastfetch → Task 7. ✅
- §8 ⌘K + arte ASCII → Task 8. ✅
- §9 Pipeline ASCII → Task 1. ✅
- §10 datos/config → Tasks 4,7,8. ✅
- §11 rendimiento/errores → Tasks 2 (rAF/IO/reduced-motion), 7/8 (degradación). ✅
- §13 criterios de aceptación → Tasks 1-9. ✅

**Notas/limitaciones:** assets reales pendientes del usuario — avatares (`public/avatars/`), arte ⌘K (`public/art/`), screenshots de proyectos (`public/projects/`). Hasta entonces se usan placeholders. Heatmap GitHub decorativo sin `GITHUB_TOKEN`+GraphQL.
